package com.ifmt.gerenciamentoMudas.Repository;

/*
 * import org.springframework.data.jpa.repository.JpaRepository;
 * import org.springframework.data.jpa.repository.Query;
 * import org.springframework.stereotype.Repository;
 * 
 * import com.ifmt.gerenciamentoMudas.Model.Localizacao;
 * import com.ifmt.gerenciamentoMudas.Model.MicroControlador;
 * import com.ifmt.gerenciamentoMudas.Model.Plant;
 * 
 * import java.util.List;
 * 
 * @Repository
 * public interface LocalizacaoRepository extends JpaRepository<Localizacao,
 * Long> {
 * 
 * @Query("SELECT a.microControladorList FROM Localizacao a WHERE a.id = :id")
 * List<MicroControlador> buscarListaMicroControladorApartirIdLocalizacao(Long
 * id);
 * 
 * @Query("SELECT a.sementeList FROM Localizacao a WHERE a.id = :id")
 * List<Semente> buscarSementeApartirIdLocalizacao(Long id);
 * }
 */
